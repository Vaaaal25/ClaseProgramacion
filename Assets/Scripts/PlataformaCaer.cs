using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlataformaCaer : MonoBehaviour
{
  
    //[SerializeField] private float tiempoEspera;
    //private Rigidbody rb;
    //[SerializeField] private float velocidadRotacion;
    //private Animator animator;
    //private bool caida = false;

    //void Start()
    //{
      // rb = GetComponent<Rigidbody>();
      // animator = GetComponent<Animator>(); 
    //}

    //private void OnCollisionEnter(Collision other)
   // {
       // if (other.gameObject.CompareTag("Player"))
       //{
       //     StartCoroutine(Caida(other));
        //}
  //  }

   // private IEnumerator Caida(Collision other)
   // {
      //  void return new WaitForSeconds(tiempoEspera);
       // caida = true;
       // Physics.IgnoreCollision(transform.GetComponent<collider>(), other.transform.GetComponent<collider>());
       // rb.constraints = RigidbodyConstraints.None;
       // rb.AddForce(new Vector3(0.1f, 0f, 0f));
  //  }

    void Update()
    {
        
    }
}
